machine:
  environment:
    BROWSER_NAME: 'chrome'
    TEST_SERVER_PORT: '4466'
dependencies:
  pre:
    - ./setup.sh
  post:
    - cp ./node_modules/scribe-test-harness/bin/run-tests.sh .
    - bower install